<h1>Wishlist</h1>

<div class="wishlist">
  {{#each records}}
  <div class="album-card">
    <h2>{{artists_sort}}</h2>
    <img src="{{thumb}}" alt="">
    <h3>{{title}}</h3>
    <a href="/artist/{{id}}/deleteWishList"><button>Remove from Wishlist</button></a>
  </div>
  {{/each}}
</div>