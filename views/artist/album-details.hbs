<div class="info">
  <div class="album-cover">
  <img src="{{coverImg}}" alt="albumcover {{albumDetails.title}}">
  {{#if user}}
  {{#if main_release}}
    <a href="/artist/{{albumDetails.main_release}}/albumStats"><button>People who like this</button></a>
  {{else}}
    <a href="/artist/{{albumDetails.id}}/albumStats"><button>People who like this</button></a>
  {{/if}}
{{/if}}
  </div>
  <div class="album-details">
  <div>
    <h1>{{albumDetails.artists_sort}}</h1>
    <h2>{{albumDetails.title}}</h2>
    <h3>{{albumDetails.year}}</h3>
  </div>
 <div class="tracks-list">
<ol>
  {{#each tracklist}}
  <li>{{title}}{{#if duration}} – {{duration}}{{/if}}</li>
  {{/each}}
</ol>
</div> 
<div>
<p>{{albumDetails.notes}}</p>
</div> 
</div>
</div>



